const SHUFFLE = "SHUFFLE";
const SOLVE = "SOLVE";
const STEP = "STEP";
const UPDATE_INPUT = "UPDATE_INPUT";

const cubeReducer = (state, action) => {
    switch(action.type){
        case SHUFFLE:
            state.
    }
}